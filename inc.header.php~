<?php
/*
	Page: inc.header.php
	Desc: Prepares pages\menus then displays HTML
*/
include_once 'config.php';

// startup and check
session_start();

// set page: if logged in, set requested, else set to index
if(isset($_GET['p']) && is_valid_user()) $page = $_GET['p'];
else $page = "index";


?>
